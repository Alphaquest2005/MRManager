using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace MRManager_UnitTests
{
    [TestClass]
    public class Development
    {
        //[TestMethod]
        //public void GetStaticMethodwithGenericTypes()
        //{
        //    //Expression<Func<Media, SignInInfo>>
        //    //var expression = MediaExpressions.MediaToSignInInfoExpression;

        //    var rt = typeof(Expression<Func<Media, SignInInfo>>);

        //    var types = typeof(FindExpressionClass).Assembly.ExportedTypes
        //        .Where(t => t
        //            .GetMethods(
        //                ).Any(m => m.ReturnType == rt));
        //    var res = types.First().GetMethods().FirstOrDefault(x => x.ReturnType == rt)?.Invoke(null,null);

        //}

        //[TestMethod]
        //public void GetBaseType()
        //{
        //    var bt = typeof(ISignInInfo).GetInterfaces().FirstOrDefault(x => x.Name.Contains("IEntityView"));
        //    var res = bt.GetGenericArguments().First();
        //      //  return res;
           
        //  //  return bt;
        //}

        //[TestMethod]
        //public void TestSigninExpression()
        //{
        //    using (var ctx = new MRManagerDBContext())
        //        {
        //           // var res = ctx.Set<Media>().Select(MediaExpressions.MediaToSignInInfoExpression).FirstOrDefault(x => x.Id == 1);

                    
        //        }

        //}
    }
}
